# Generated by agent-sandbox. Re-rendered on bind-cluster.
# Aggregates into the standard `view` ClusterRole. Listed CRDs are enumerated at
# provision time from `kubectl get crds`. The standard view role's exclusions
# (secrets, etc.) remain — only the per-cluster CRDs listed here get added.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{CLUSTER_ROLE_NAME}}
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
    app.kubernetes.io/managed-by: agent-sandbox
    agent-sandbox.cluster: {{CLUSTER_NAME_LABEL}}
rules:
{{RULES}}
